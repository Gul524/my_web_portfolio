import 'package:flutter/material.dart';

class D_ContactSection extends StatelessWidget {
  const D_ContactSection ({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 600,
      width: double.infinity,
      
    );
  }
}